# Agents

# Next.js: ALWAYS read docs before coding

Before any Next.js work, find and read the relevant doc in `node_modules/next/dist/docs/`. Your training data is outdated — the docs are the source of truth.

# PayloadCMS: ALWAYS read docs before coding

If you're working with PayloadCMS, always read the docs before coding.
This project uses the Payload CMS skill at `.agents/skills/payload/`.
Start with `.agents/skills/payload/SKILL.md` for a quick reference, then see `.agents/skills/payload/reference/` for detailed docs.

# DESIGN.md: ALWAYS read before coding

Before any design work, always read `DESIGN.md` for a detailed description of the system design of the project.
